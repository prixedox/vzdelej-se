# src/components/ui/

shadcn/ui primitives. Style: **new-york**.

## IMPORTANT: Never edit these files manually

Add new components: `pnpm dlx shadcn@latest add <name>`
Config: `components.json` in project root.

Available: avatar, badge, button, card, dialog, dropdown-menu, input, label, progress, separator, sheet, sonner, tabs
